# 로그 기반 저비용 Taint 평가 설계

이 문서는 [감사 로그 기반 C-/D-Taint 연구 설계](RESEARCH_REDESIGN.md)의 비용 가설과 측정 방법을
정의한다. 기존 업무 요청은 변경하지 않으며 비용은 선택적으로 수집·처리한 이벤트에 대해서만 발생한다.

## 1. 비용 가설

- H1: 접촉하지 않은 지뢰의 한계비용은 작은 seed와 registry 저장비에 가깝다.
- H2: Taint 분석비는 전체 업무 요청 수가 아니라 선택된 감사 이벤트 수와 실제 상태 전이 수에 비례한다.
- H3: 전체 S3 데이터 이벤트를 수집하는 구성보다 지뢰·중요 자산으로 범위를 제한한 구성이 낮은 비용으로 동일한 실험 교차점을 찾는다.
- H4: 원본 로그는 S3에 보존하고 변경된 label과 최초 교차점만 상태 DB에 저장하면 전체 이벤트를 검색 DB에 복제하는 것보다 저장비가 작다.

```text
C_total = C_management_log_reuse
        + N_seed * C_seed_storage
        + R_scoped * C_data_event
        + B_batch * C_analysis
        + T_changed * C_state_write
        + X_alert * C_notification
        + C_retention
```

`N_seed`는 지뢰 수, `R_scoped`는 선택 범위에 들어온 이벤트 수, `T_changed`는 실제 label 변화 수다.

## 2. 비교군

| 구성 | 설명 | 목적 |
|---|---|---|
| A 관리 로그만 | 기본 CloudTrail 관리 이벤트 | 기존 고정비 확인 |
| B 전체 데이터 이벤트 | 연구 계정 S3 데이터 이벤트 전체 | 비용 상한 비교군 |
| C 선택 데이터 이벤트 | HoneyToken·중요 자산 범위만 수집 | 제안 구성 |
| D 선택 수집+Taint | C에 정규화·전파·상태 저장 추가 | 제안 기법의 순수 추가비 측정 |

같은 기간, 같은 API workload, 같은 보존기간을 사용한다. 기존에 수행한 별도 실행 경로 실험의
지연·호출 비용은 이 비교에 포함하지 않는다.

## 3. 실험 변수

- 지뢰 수 `N={0,1,10,100,1000}`
- 정상 이벤트와 관련 이벤트의 비율
- S3 데이터 이벤트 선택 범위
- 배치 주기와 이벤트 묶음 크기
- 원본 로그 보존기간과 상태 보존기간
- C/D 원인 수와 edge 수
- 즉시 경보 대상과 배치 분석 대상의 비율

지뢰 수를 늘릴 때 실제 AWS 자원 수가 함께 증가하는지, 또는 객체·registry 항목만 증가하는지를 분리해
기록한다. 동일한 미끼를 복제해 공격 표면을 과장하지 않도록 고유 seed ID와 위치를 manifest에 남긴다.

## 4. 측정값

- CloudTrail 관리·데이터 이벤트 수와 전달 byte
- S3 PUT/GET/LIST, 저장 byte-month와 KMS 요청 수
- 분석 실행 횟수, 실행 시간, 메모리 및 처리 이벤트 수
- 상태 DB read/write와 저장 byte
- 지뢰 접촉부터 C 생성, C∩D 판정까지의 지연
- 전체 이벤트 중 분석 대상 비율과 label이 실제로 변한 비율
- E1 전파·교차의 precision/recall 및 E2 후보 과도 전파율
- 백만 업무 API 또는 백만 수집 이벤트당 예상 추가비

짧은 실행의 Cost Explorer 청구액만 사용하지 않는다. 실제 API 수와 처리시간에 실험일의 공식 리전
단가를 적용한 bottom-up 추정치를 만들고, 24시간 이상 반복 실험에서는 CUR/Cost Explorer와 대조한다.

## 5. 실행 절차

1. 동일 합성 workload와 정답 manifest를 고정한다.
2. A~D 구성을 독립 시간창 또는 독립 trail로 실행한다.
3. B0, C1, C2, D1, D2, CD1, CD2, N1~N3를 조건별 반복한다.
4. CloudTrail 전달 완료 기준을 충족한 뒤 동일 분석 버전으로 처리한다.
5. 원본 로그 수, 상태 전이 수, 교차점과 AWS 사용량을 수집한다.
6. 구성별 precision/recall, 지연, 비용을 계산한다.
7. 임시 지뢰와 객체는 정확한 run ID와 version을 확인한 후 정리하고, 비식별 결과와 원본 증거 보존 정책을 기록한다.

## 6. 통과 기준

- C와 D에서 B와 C의 E1 결과가 동일해야 한다.
- 선택 범위 밖 정상 이벤트 때문에 false C/D 전파가 생기지 않아야 한다.
- CD1·CD2의 최초 E1 교차점을 찾고 N1~N3을 E1 교차로 잘못 분류하지 않아야 한다.
- 구성 D의 추가비가 이벤트 수, 상태 변화 수와 함께 재현 가능하게 설명되어야 한다.
- 비용 감소를 주장하려면 구성 C 또는 D가 B보다 실제로 저렴하고 필요한 E1 coverage를 유지해야 한다.

## 7. 해석 제한

선택 수집은 비용을 낮추지만 범위 밖 자산의 행위를 볼 수 없다. 따라서 비용 결과와 함께 관측 coverage를
반드시 보고한다. 무료 사용량, 일시적 할인, 작은 payload 또는 유휴 계정의 결과를 기업 규모의 절대 비용으로
일반화하지 않는다. 비용 절감률은 선택한 리소스 범위와 workload 분포를 함께 공개할 때만 의미가 있다.
